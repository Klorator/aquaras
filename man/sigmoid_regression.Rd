% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sigmoid_regression.R
\name{sigmoid_regression}
\alias{sigmoid_regression}
\title{Fit and Plot a Sigmoidal Regression Curve}
\usage{
sigmoid_regression(
  data,
  x_name,
  y_name,
  sem_name = NULL,
  outlier_indices = NULL,
  point_color = "black",
  point_shape = 16,
  point_size = 3,
  excluded_point_color = "red",
  excluded_point_shape = 16,
  line_color = "blue",
  line_type = "solid",
  line_size = 1,
  ci_fill = "blue",
  text_size = 16,
  x_label = NULL,
  y_label = NULL
)
}
\arguments{
\item{data}{A data frame containing all columns used for fitting and plotting.}

\item{x_name}{A string naming the numeric predictor column in \code{data}.}

\item{y_name}{A string naming the numeric response column in \code{data}.}

\item{sem_name}{Optional string naming the SEM column in \code{data}. If provided,
y +/- SEM error bars are added.}

\item{outlier_indices}{Optional integer vector of row indices in \code{data} to
exclude from model fitting.}

\item{point_color}{Color for included data points and their error bars.}

\item{point_shape}{Shape for included data points.}

\item{point_size}{Size for all points.}

\item{excluded_point_color}{Color for excluded points and their error bars.}

\item{excluded_point_shape}{Shape for excluded data points.}

\item{line_color}{Color of the fitted sigmoidal line.}

\item{line_type}{Linetype of the fitted line (e.g., \code{"solid"}, \code{"dashed"}).}

\item{line_size}{Line width of the fitted line.}

\item{ci_fill}{Fill color of the 95\% confidence interval ribbon.}

\item{text_size}{Base text size used in \code{\link[ggplot2:ggtheme]{ggplot2::theme_classic()}}.}

\item{x_label}{Optional custom x-axis label. Defaults to \code{x_name} when \code{NULL}.}

\item{y_label}{Optional custom y-axis label. Defaults to \code{y_name} when \code{NULL}.}
}
\value{
A named list with:
\describe{
\item{\code{model}}{The fitted \code{nls} model object.}
\item{\code{coefficients}}{Named numeric vector of fitted coefficients.}
\item{\code{plot}}{A \code{ggplot} object containing points, fit, and confidence band.}
\item{\code{predicted_curve}}{Data frame with x values and fitted/CI values.}
\item{\code{fit_data}}{Data used for model fitting (after exclusions).}
\item{\code{excluded_data}}{Data excluded from fitting and highlighted in the plot.}
}
}
\description{
Fits a logistic (sigmoidal) curve using \code{\link[stats:nls]{stats::nls()}} with \code{\link[stats:SSlogis]{stats::SSlogis()}},
optionally excluding selected rows from model fitting while still displaying them
on the plot. Included and excluded points can be styled independently, and optional
SEM error bars and a 95\% confidence interval ribbon are added to the plot.
}
\examples{
my_data <- data.frame(
  logd = c(-2, -1, 0, 1, 2, 3),
  mean = c(0.98, 0.90, 0.60, 0.25, 0.10, 0.05),
  sem = rep(0.02, 6),
)

outliers <- c(4, 6)
my_data_result <- sigmoid_regression(
  data = my_data,
  x_name = "logd",
  y_name = "mean",
  sem_name = "sem",
  outlier_indices = outliers
)

my_data_result$coefficients
my_data_result$plot

}
